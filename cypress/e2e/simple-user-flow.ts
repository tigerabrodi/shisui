import { buildQuestionAnswers } from "../support/generate";

beforeEach(() => {
  cy.clearCookies();
  cy.visit("/");
});

it("Simple user flow, tested via daily questions.", () => {
  /*   const {firstAnswer, firstQuestion, secondAnswer, secondQuestion} = buildQuestionAnswers() */
});
